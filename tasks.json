{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "py: run current file",
      "type": "shell",
      "command": "start=$(date +%s); ${config:python.defaultInterpreterPath} \"${file}\" --verbose True; end=$(date +%s); elapsed=$((end - start)); echo; printf \"Elapsed: %02d:%02d:%02d\\n\" $((elapsed/3600)) $(((elapsed%3600)/60)) $((elapsed%60));",
      "options": {
        "cwd": "${workspaceFolder}",
        "shell": {
          "executable": "/bin/zsh",
          "args": [
            "-i",
            "-lc"
          ]
        },
        "env": {
          "PYTHONPATH": "${env:PYTHONPATH}:${workspaceFolder}:${workspaceFolder}/src:${workspaceFolder}/src/backend"
        }
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "problemMatcher": []
    }
  ]
}